import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'food-recipes-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
